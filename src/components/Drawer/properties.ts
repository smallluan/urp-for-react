import { DrawerProps } from "./type"

export const defaultProps: DrawerProps = {
  className: '',
  style: {},
  attach: 'body',
  position: 'left',
  closeBtn: true,
  confirmBtn: true,
  cancelBtn: true,
  zIndex: 1000,
}
