import { SwiperProps } from "./type"

export const swiperDefaultProps: SwiperProps = {
  className: '',
  style: {},
  autoplay: true,
  defaultCurrent: 0,
  direction: 'horizontal',
  duration: 300,
  interval: 4000,
  loop: false,
  stopOnHover: true,
}
